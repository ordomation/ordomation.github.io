---
---
<section class="section section-dark project-section">
  <div class="container project-container">
    <div class="project-content">
      <span class="project-label">CURRENT PROJECT</span>
      <h2 class="project-title">The Email Library</h2>
      <p class="project-description">
        We're building an open-source email archival and classification system designed for Danish small businesses.
      </p>
      <p class="project-description">
        It monitors Gmail accounts, extracts invoices and receipts, classifies them using AI, and organizes them into a structured archive ready for your accountant.
      </p>
      
      <div class="tech-stack">
        <span class="badge stack-badge">n8n</span>
        <span class="badge stack-badge">PostgreSQL</span>
        <span class="badge stack-badge">Groq AI</span>
        <span class="badge stack-badge">TrueNAS</span>
      </div>

      <a href="https://github.com/ordomation" target="_blank" class="btn btn-primary cta-btn">View Architecture</a>
    </div>
    
    <div class="project-visual">
      <div class="visual-placeholder">
        <!-- Minimal geometric visual representing data flow -->
        <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" class="visual-svg">
          <circle cx="100" cy="100" r="80" fill="none" stroke="var(--slate)" stroke-width="2" stroke-dasharray="4 4" />
          <circle cx="100" cy="100" r="40" fill="none" stroke="var(--electric)" stroke-width="4" />
          <circle cx="100" cy="100" r="8" fill="var(--white)" />
          <path d="M100 20 L100 60 M100 140 L100 180 M20 100 L60 100 M140 100 L180 100" stroke="var(--electric)" stroke-width="2" stroke-linecap="round" />
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  .project-section {
    padding: 128px 0;
  }

  .project-container {
    display: flex;
    align-items: center;
    gap: var(--space-3xl);
  }

  .project-content {
    flex: 0 0 60%;
  }

  .project-visual {
    flex: 0 0 40%;
    display: flex;
    justify-content: center;
  }

  .project-label {
    display: inline-block;
    color: var(--electric);
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: var(--space-sm);
  }

  .project-title {
    margin-bottom: var(--space-md);
  }

  .project-description {
    color: var(--cloud);
    opacity: 0.9;
    font-size: 18px;
    max-width: 600px;
  }

  .tech-stack {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    margin: var(--space-lg) 0;
  }

  .stack-badge {
    background-color: var(--slate);
    color: var(--white);
    padding: 6px 16px;
  }

  .cta-btn {
    margin-top: var(--space-sm);
  }

  .visual-placeholder {
    width: 100%;
    max-width: 300px;
    aspect-ratio: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .visual-svg {
    width: 100%;
    height: 100%;
    animation: rotateSlowly 20s linear infinite;
  }

  @keyframes rotateSlowly {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @media (max-width: 1024px) {
    .project-container {
      flex-direction: column;
      gap: var(--space-xl);
    }
    
    .project-content, .project-visual {
      flex: 1 1 100%;
      text-align: left;
    }
    
    .project-visual {
      margin-top: var(--space-lg);
    }
  }
</style>
