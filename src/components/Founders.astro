---
---
<section class="section founders-section" id="story">
  <div class="container">
    <div class="founders-intro">
      <h2 class="founders-headline">Built for the exact problems we <span class="gradient-text">faced.</span></h2>
    </div>

    <div class="story-grid">
      
      <!-- Left side: Photo Placeholder -->
      <div class="story-visual group">
        <div class="photo-placeholder glass-panel">
          <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" class="photo-icon" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><circle cx="8.5" cy="8.5" r="1.5"></circle><polyline points="21 15 16 10 5 21"></polyline></svg>
          <span class="photo-label muted">Team Photo Placeholder</span>
          <div class="photo-glow"></div>
        </div>
      </div>

      <!-- Right side: Origin Story -->
      <div class="story-content">
        <p class="story-text">
          We built GreenGuard Automation from the ground up, scaling it into a successful venture. But as we grew, we realized that administrative overhead—specifically managing receipts, matching invoices, and prepping VAT—was killing our momentum.
        </p>
        <p class="story-text">
          We spent our weekends doing data entry instead of building our business. We tried existing tools, but they were either too complex, too expensive, or just didn't work reliably with Danish invoices.
        </p>
        <p class="story-text highlight">
          So we built Ordomation. The tool we desperately needed for our own company.
        </p>

        <div class="founders-signatures">
          <div class="signature-block">
            <h4 class="founder-name">Thue Tonnesen</h4>
            <span class="founder-role muted">Co-Founder, Infrastructure</span>
          </div>
          <div class="signature-block">
            <h4 class="founder-name">Joachim Ovesen</h4>
            <span class="founder-role muted">Co-Founder, Strategy</span>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  .founders-section {
    padding: 120px 0;
    background-color: var(--bg-primary);
    position: relative;
    overflow: hidden;
  }

  .founders-intro {
    margin-bottom: var(--space-3xl);
  }

  .founders-headline {
    font-size: clamp(36px, 5vw, 56px);
    max-width: 600px;
    line-height: 1.1;
  }

  .story-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  /* Photo Placeholder */
  .story-visual {
    position: relative;
  }

  .photo-placeholder {
    aspect-ratio: 4/3;
    width: 100%;
    border-radius: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-md);
    position: relative;
    overflow: hidden;
    /* initial state for GSAP */
    opacity: 0;
    transform: x(-30px);
  }

  .photo-icon {
    color: var(--text-muted);
    opacity: 0.5;
    transition: transform 0.5s ease;
  }

  .photo-placeholder:hover .photo-icon {
    transform: scale(1.1);
    color: var(--accent);
    opacity: 1;
  }

  .photo-label {
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 14px;
  }

  .photo-glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(139, 92, 246, 0.15) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  .photo-placeholder:hover .photo-glow {
    opacity: 1;
  }

  /* Story Content */
  .story-content {
    /* initial state for GSAP */
    opacity: 0;
    transform: x(30px);
  }

  .story-text {
    font-size: 18px;
    line-height: 1.7;
    color: var(--text-secondary);
    margin-bottom: var(--space-lg);
  }

  .story-text.highlight {
    color: var(--text-primary);
    font-weight: 500;
    font-size: 20px;
    border-left: 2px solid var(--accent);
    padding-left: var(--space-md);
    margin-top: var(--space-xl);
  }

  .founders-signatures {
    display: flex;
    gap: var(--space-2xl);
    margin-top: var(--space-2xl);
    padding-top: var(--space-xl);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .founder-name {
    font-size: 18px;
    margin: 0 0 4px 0;
    color: var(--text-primary);
  }

  .founder-role {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @media (max-width: 900px) {
    .story-grid {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    
    .founders-headline {
      text-align: center;
      margin: 0 auto var(--space-2xl);
    }

    .founders-signatures {
      flex-direction: column;
      gap: var(--space-md);
    }
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  document.addEventListener('DOMContentLoaded', () => {
    gsap.registerPlugin(ScrollTrigger);

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: '.story-grid',
        start: 'top 75%',
      }
    });

    tl.to('.photo-placeholder', {
      opacity: 1,
      x: 0,
      duration: 1,
      ease: 'power3.out'
    })
    .to('.story-content', {
      opacity: 1,
      x: 0,
      duration: 1,
      ease: 'power3.out'
    }, "-=0.6");
  });
</script>
